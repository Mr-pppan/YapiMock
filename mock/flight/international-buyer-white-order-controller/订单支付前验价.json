{
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "description": "返回编码"
    },
    "isSuccess": {
      "type": "string",
      "description": "是否可支付，可以支付：success，不可以支付：fail"
    },
    "msg": {
      "type": "string",
      "description": "返回信息"
    }
  },
  "title": "InterPayBeforeCheckPriceResponse",
  "$$ref": "#/definitions/InterPayBeforeCheckPriceResponse"
}